name: Example Workflow
on:
  workflow_dispatch:
  schedule:
    - cron: "0 0 * * *"

jobs:
  create_folder:
    runs-on: ubuntu-latest

    steps:
     - name: Checkout Repository
       uses: actions/checkout@v2
     - name: Create Folder
       run: |
          mkdir hello
          dir
